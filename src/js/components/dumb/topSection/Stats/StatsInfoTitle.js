import React from 'react';

const StatsInfoTitle = props => {
    return(
        <div  className="stats-year">
            For the year {props.year}
        </div>
    )
}

export default StatsInfoTitle;